<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Worker Profile</title>
    <link rel="stylesheet" href="styles.css">
  </head>
  <body>
    <div class="profile-card">
      <div class="top-row">
        <span class="profile-image">
          <img src={{avatar}} alt="{{name}}'s image">
        </span>
        <span class="profile-info">
          <h1 class="profile-name">{{name}}</h1>
          <p class="profile-address">{{address.city}}, {{address.country}} | {{job}}, {{company}}</p>
          <p class="profile-contact">{{email}} | {{phone}}</p>
        </span>
      </div>
      <div class="bottom-row">
        <span class="profile-headers">
          <h3>Education</h3>
          <h3>Skills</h3>
      </span>
        <span class="profile-edu-skills">
          <p class="profile-edu">{{university}}</p>
          <div class="profile-skills">
            {{#skills}}
            <p>{{.}}</p>
            {{/skills}}
          </div>
        </span>
      </div>
    </div>
  </body>
</html>
